var placementIds = {
    sdk_gdt_placementid : '${sdk_gdt_placementid}',
    sdk_gdt_displayType: '${sdk_gdt_displayType}'
}

var taskInfo = {
    tasks: ${TASKLIST},
    timeouts: ${TIMEOUTS},
    traceid: '${TRACEID}',
}
